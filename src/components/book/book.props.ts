export interface BookProps {
  imageSrc: string,
  title: string,
  description: string
}
//Props are like Parameters for a component, TypeScript checks if all Parameter are available when component is used